# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(lidar_ousterTaskLib)
ADD_LIBRARY(${LIDAR_OUSTER_TASKLIB_NAME} SHARED
    ${LIDAR_OUSTER_TASKLIB_SOURCES})
add_dependencies(${LIDAR_OUSTER_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${LIDAR_OUSTER_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${LIDAR_OUSTER_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${LIDAR_OUSTER_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${LIDAR_OUSTER_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${LIDAR_OUSTER_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${LIDAR_OUSTER_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${LIDAR_OUSTER_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${LIDAR_OUSTER_TASKLIB_HEADERS}
    DESTINATION include/orocos/lidar_ouster)

